spring:
  application:
    name: config-server

  profiles:
    active: native

  #CONFIG SERVER
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config

server:
  port: 8888